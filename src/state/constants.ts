/** Maximum session events retained in renderer memory. */
export const MAX_EVENTS = 256

/** Maximum events stored per session before trimming oldest entries. */
export const MAX_EVENTS_PER_SESSION = 96
